export function RosterDisclaimer() {
  return (
    <p className="mt-3 max-w-2xl text-xs leading-relaxed text-faint">
      Not an official speedrun.com board. Times are Gauntlet-verified VODs or
      attributed speedrun.com API imports — every imported row links back.
    </p>
  );
}
